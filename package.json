{
    "name": "offline-first-poc",
    "private": true,
    "version": "0.0.0",
    "workspaces": [
        "packages/*",
        "apps/*",
        "server*"
    ],
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "lint": "turbo run lint"
    }
}