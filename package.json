{
    "name": "offline-first-poc",
    "private": true,
    "version": "0.0.0",
    "workspaces": [
        "packages/*",
        "apps/*",
        "server"
    ],
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "lint": "turbo run lint"
    },
    "dependencies": {
        "turbo": "^2.4.2"
    },
    "packageManager": "npm@10.8.3",
    "devDependencies": {
        "@types/multer": "^2.1.0"
    }
}
